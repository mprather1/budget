<%= @account.account_number %>
<%= @account.balance %>

<%= form_tag(controller: 'accounts', action: 'debit', id: session[:user_id] ) do |f| %>
  <%= text_field_tag(:transaction) %>
  <%= submit_tag("Submit") %>
<% end %>
